# Tests

`semver.rz` covers strict parsing, the canonical SemVer prerelease precedence chain, build-metadata precedence behavior, invalid leading zeroes, and representative package requirement forms.
