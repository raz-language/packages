# Tests

`datetime.rz` covers Gregorian leap-year/calendar conversion, pre-epoch dates, weekday calculation, RFC 3339 parsing/formatting, UTC-offset conversion, duration arithmetic, timestamp arithmetic, and malformed-date rejection.
